@model IEnumerable<LSA.Entities.Tasting>

@{
    ViewData["Title"] = "All";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>All Tastings</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TastingName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsFinished)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.TastingName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsFinished)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.TastingId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.TastingId">Details</a> |
                    <a asp-action="TastingResult" asp-route-id="@item.TastingId">Tasting Result</a> |
                    <a asp-action="Delete" asp-route-id="@item.TastingId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

